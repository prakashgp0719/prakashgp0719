These has to be installed in terminal for execution of the program
pip install smtplib
pip install pyttsx3
pip install speech recognition
pip install pyaudio    (if any error occured while installing pyaudio please intall python 3.6 interpreter)
 
